const config = {
    // 数据库配置
    mysql: {
        host: 'localhost',
        user: 'root',
        password: 'root123456',
        database: 'saas_basic',
        port: 3306,
    },
};

export default config;
